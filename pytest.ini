[pytest]
testpaths =
    test
    physics/test
markers =
    unit: fast unit tests for isolated logic
    visual: screenshot-based rendering regression tests
    network: tests that require live network access to external repositories
