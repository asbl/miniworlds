# Positions > Rect

`Rect` represents rectangular positions and sizes. It is exported by
`miniworlds` for code that needs direct rectangle calculations.

## API Reference

```{eval-rst}
.. autoclass:: miniworlds.positions.rect.Rect
   :members:
```

